name: Keep Alive
on:
  schedule:
    - cron: '*/5 * * * *' # GitHub Actions 最短只能是5分钟

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://zxm1010-cliproxyapi.hf.space > /dev/null
