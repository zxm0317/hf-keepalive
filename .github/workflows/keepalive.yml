name: Keep HF Space Alive

on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -L --max-time 60 --retry 3 --retry-delay 10 \
            https://zxm1010-api-cpa2.hf.space/
